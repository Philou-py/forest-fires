// import type { Cell } from "$lib/fireGrid";
// import type { PageLoad } from "./$types";

// export const load: PageLoad = async ({ fetch }) => {
//   const response = await fetch("/api/load-maps");
//   const result: { grid: Cell[][], imageDataArray: number[] } = await response.json();
//   return result;
// };
